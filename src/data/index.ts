import { topics } from './topics';
import { lessons } from './lessons';
import { questions } from './questions';

export const sampleData = {
  topics,
  lessons,
  questions,
};

// Re-export individual arrays if needed elsewhere
export { topics, lessons, questions };
